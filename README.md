

## 个人练习项目

##第一个项目，主要是个人练习用

3.27 完成vector (100/100)

3.31 完成list  (100/100)

## 当前进度

这是我跟着 `MyTinySTL` 练习手写 STL 的阶段性记录。

### 已完成
- `vector`
- `list`
- `construct`
- `allocator`

### 当前状态
- `vector` 测试通过：**100%**
- `list` 测试通过：**100%**
- `iterator` 测试通过 **100%** 但掌握不完全，仅看懂
- `construct`：已重写
- `allocator`：已重写

### 说明
目前已经完成顺序容器中的 `vector`、`list`，并重写了底层对象构造与内存管理相关组件 `construct`、`allocator`。  
现阶段重点在于梳理 STL 容器的底层实现逻辑，包括：

- 原始内存的申请与释放
- 对象的构造与析构
- 容器插入、删除、扩容时的生命周期管理

